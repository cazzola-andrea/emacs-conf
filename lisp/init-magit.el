(use-package dash
  :ensure t)

(use-package with-editor
  :ensure t)

(use-package magit)

(use-package magit-popup
  :ensure t)

;; Required for magit
;; (use-package libgit
;;   :ensure t)
  
(provide 'init-magit)
